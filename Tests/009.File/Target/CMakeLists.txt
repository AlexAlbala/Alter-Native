CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(FileProj CXX)
ADD_SUBDIRECTORY(System)
ADD_SUBDIRECTORY(gc)
SET(EXECPATH Program.cpp Program.h main.cpp Program.cpp Program.h main.cpp)
ADD_EXECUTABLE(File ${EXECPATH})
TARGET_LINK_LIBRARIES(File System)
TARGET_LINK_LIBRARIES(File gc-lib)
IF(UNIX)
IF(!ANDROID)
TARGET_LINK_LIBRARIES(File pthread)
ENDIF()
ENDIF(UNIX)
