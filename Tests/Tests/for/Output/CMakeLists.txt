CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(ForProj CXX)
ADD_SUBDIRECTORY(System)
SET(EXECPATH Program.cpp Program.h main.cpp Program.cpp Program.h main.cpp)
ADD_EXECUTABLE(For ${EXECPATH})
TARGET_LINK_LIBRARIES(For LIB)
IF(UNIX)
IF(!ANDROID)
TARGET_LINK_LIBRARIES(For pthread)
ENDIF()
ENDIF(UNIX)
