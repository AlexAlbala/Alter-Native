CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(FileStreamProj CXX)
ADD_SUBDIRECTORY(System)
ADD_SUBDIRECTORY(gc)
SET(EXECPATH Test.cpp Test.h main.cpp Test.cpp Test.h main.cpp)
ADD_EXECUTABLE(FileStream ${EXECPATH})
TARGET_LINK_LIBRARIES(FileStream System)
TARGET_LINK_LIBRARIES(FileStream gc-lib)
IF(UNIX)
IF(!ANDROID)
TARGET_LINK_LIBRARIES(FileStream pthread)
ENDIF()
ENDIF(UNIX)
